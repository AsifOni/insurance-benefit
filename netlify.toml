[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  publish = ".next"
  command = "npm run build"

[context.preview]
  command = "npm run build"
  environment = { NODE_ENV = "development" }
  publish = ".next"